#include<stdio.h>
#include<stdlib.h>
main()
{
	float x, y, z, e;
	printf("insira o valor de x: ");
	scanf("%f", &x);
	y=sin(x)+pow(x,5)-fabs(pow(x,2));
	z=cos(x)-pow(e,x);
	system("pause");
	return(0);
}
